class MemberService {
    init = () => {

    }
    authorizeMember = async () => {
        let parts = window.location.href.split("/");
        if (parts[4] !== null) {
            let slugUrl = parts[4];

        }
    }
}